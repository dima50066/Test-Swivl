import styled from 'styled-components';

export const FeedWrapper = styled.div`
  padding-top: 80px;
  position: relative;
  display: flex;
  flex-wrap: wrap;
  gap: 24px;
  justify-content: unset;
`;
